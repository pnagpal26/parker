---
phase: 01-pre-production
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-pre-production/NANO-BANANA-VERIFY.md
  - .planning/phases/01-pre-production/META-HOUSING-BRIEF.md
  - .planning/phases/01-pre-production/FIGMA-TEMPLATE-SPEC.md
autonomous: false
requirements:
  - PRE-02
  - PRE-04

must_haves:
  truths:
    - "nano-banana (Gemini CLI) is confirmed working — a test image exists in ./nanobanana-output/"
    - "The exact CLI command for 9:16 story backgrounds is documented and verified"
    - "Meta Housing Special Ad Category requirements are documented for handoff to campaign manager"
    - "Figma canvas template spec is documented so the human can set it up before Phase 2"
  artifacts:
    - path: ".planning/phases/01-pre-production/NANO-BANANA-VERIFY.md"
      provides: "CLI verification results — PRE-02 gate, exact commands for Phase 2"
    - path: ".planning/phases/01-pre-production/META-HOUSING-BRIEF.md"
      provides: "Campaign manager handoff doc for Housing Special Ad Category — PRE-04 gate"
    - path: ".planning/phases/01-pre-production/FIGMA-TEMPLATE-SPEC.md"
      provides: "Exact Figma setup instructions for 1080×1920 safe-zone template"
  key_links:
    - from: "NANO-BANANA-VERIFY.md"
      to: "Phase 2 background generation tasks"
      via: "CLI command referenced verbatim in every Phase 2 generation task"
    - from: "META-HOUSING-BRIEF.md"
      to: "Phase 3 Campaign Handoff"
      via: "PRE-04 stub that Phase 3 plan 03-02 will expand"

user_setup:
  - service: nano-banana (Gemini CLI)
    why: "CLI tool for AI background image generation — required for Phase 2"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio → API Keys → Create API Key"
    dashboard_config:
      - task: "Install Gemini CLI if not in PATH"
        location: "Terminal: npm install -g @google/generative-ai-cli OR follow https://github.com/google-gemini/gemini-cli"
      - task: "Install nano-banana extension if not listed"
        location: "Terminal: gemini extensions install https://github.com/gemini-cli-extensions/nanobanana"
---

<objective>
Verify the nano-banana CLI works for 9:16 image generation, document the result and exact production command (PRE-02), create the Meta Housing Special Ad Category brief stub (PRE-04), and write the Figma canvas template spec for human setup.

Purpose: PRE-02 is a hard gate — if nano-banana cannot generate 9:16 images, Phase 2 must pivot to a Figma-only background approach. This plan determines which path Phase 2 takes. PRE-04 documents campaign setup requirements for Garima's ads team so they don't make a fatal Ads Manager error.

Output: Three docs in .planning/phases/01-pre-production/ — CLI verification record, Meta brief stub, Figma spec.
</objective>

<execution_context>
@/Users/puneetnagpal/.claude/get-shit-done/workflows/execute-plan.md
@/Users/puneetnagpal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pre-production/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run nano-banana test generation and write NANO-BANANA-VERIFY.md</name>
  <files>.planning/phases/01-pre-production/NANO-BANANA-VERIFY.md</files>
  <read_first>
    - .planning/phases/01-pre-production/01-RESEARCH.md (Nano-banana — Exact CLI Invocation section, Open Questions > GEMINI_API_KEY, Code Examples > PRE-02 Verification)
    - .planning/research/PITFALLS.md (Pitfall 4: AI-Generated Images at Wrong Resolution, Pitfall 5: nano-banana Extension Not Installed)
  </read_first>
  <action>
Run these commands in order. Record the result of each step in NANO-BANANA-VERIFY.md.

**Step 1: Check Gemini CLI is in PATH**
```bash
which gemini || echo "NOT_IN_PATH"
gemini --version 2>/dev/null || echo "VERSION_FAILED"
```

**Step 2: Check nano-banana extension is installed**
```bash
gemini extensions list 2>/dev/null | grep -i nanobanana || echo "EXTENSION_NOT_FOUND"
```

If extension not found, install it:
```bash
gemini extensions install https://github.com/gemini-cli-extensions/nanobanana
```

**Step 3: Check GEMINI_API_KEY**
```bash
[ -n "$GEMINI_API_KEY" ] && echo "API_KEY_SET" || echo "API_KEY_MISSING"
```

If missing, STOP and create the checkpoint below (do not attempt generation without key).

**Step 4: Run test generation** (only if Steps 1–3 pass)
```bash
cd /Users/puneetnagpal/development/parker
gemini --yolo "/generate 'dark luxury interior architecture, vertical portrait orientation, deep shadows, no text, no people, no logos, no watermarks'" --aspect=9:16
```

**Step 5: Verify output** (only if Step 4 completes without error)
```bash
ls -la nanobanana-output/*.png 2>/dev/null || echo "NO_OUTPUT_FILES"
sips -g pixelWidth -g pixelHeight nanobanana-output/*.png 2>/dev/null || echo "SIPS_FAILED"
```

**Write NANO-BANANA-VERIFY.md** with a template like this (fill in actual results):

```
# nano-banana CLI Verification

**Status:** [PASS / FAIL / PARTIAL]
**Date verified:** 2026-03-15
**Requirement ID:** PRE-02

## Setup Check Results

| Check | Result |
|-------|--------|
| gemini in PATH | [YES / NO — path or error] |
| nano-banana extension | [INSTALLED / NOT FOUND] |
| GEMINI_API_KEY | [SET / MISSING] |

## Test Generation

**Command used:**
```
gemini --yolo "/generate 'dark luxury interior architecture, vertical portrait orientation, deep shadows, no text, no people, no logos, no watermarks'" --aspect=9:16
```

**Result:** [SUCCEEDED / FAILED — include error message if failed]

**Output file(s):** [filename(s) in nanobanana-output/]

## Dimension Check

| File | Width | Height | Aspect | Pass/Fail |
|------|-------|--------|--------|-----------|
| [filename] | [px] | [px] | [ratio] | [PASS/FAIL] |

Note: Output at any size with correct 9:16 ratio is acceptable — Figma will
place it as a background layer within the 1080×1920 frame.

## Production Command (Phase 2 Reference)

This is the verified command pattern for Phase 2 background generation:

```
gemini --yolo "/generate '[PROMPT]'" --aspect=9:16
```

Optional for multiple variations:
```
gemini --yolo "/generate '[PROMPT]'" --aspect=9:16 --count=3
```

Output always lands in: ./nanobanana-output/ (relative to working directory)

## Fallback Path

[Complete this section if verification FAILED:]

If gemini CLI is unavailable or nano-banana extension fails:
- Phase 2 will use Fitzrovia CDN photos exclusively as background layers
  (conditional on PHOTO-RIGHTS.md written authorization being received)
- Figma compositing workflow is unchanged — only the background source changes
- Record the specific failure here so Phase 2 executor knows which path to take

## Status Gate

- [ ] Gemini CLI in PATH
- [ ] nano-banana extension installed
- [ ] GEMINI_API_KEY configured
- [ ] Test generation produced at least one file
- [ ] Output has correct 9:16 aspect ratio
```
  </action>
  <verify>
    <automated>test -f .planning/phases/01-pre-production/NANO-BANANA-VERIFY.md && grep -q "PRE-02" .planning/phases/01-pre-production/NANO-BANANA-VERIFY.md && grep -q "Production Command" .planning/phases/01-pre-production/NANO-BANANA-VERIFY.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - NANO-BANANA-VERIFY.md exists
    - Contains "PRE-02" (links to requirement)
    - Contains the setup check results table (gemini PATH, extension, API key)
    - Contains either a test generation result OR a clear Fallback Path section if CLI unavailable
    - Contains "Production Command" section with the verified command string for Phase 2
    - Status gates are filled in (checked or unchecked) with accurate results
    - If generation succeeded: at least one .png file exists in ./nanobanana-output/
  </acceptance_criteria>
  <done>NANO-BANANA-VERIFY.md is a complete record of whether the CLI works. PRE-02 is cleared if "PASS" — or the fallback path is documented if "FAIL", so Phase 2 can proceed either way.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify nano-banana output (if generation ran)</name>
  <files>nanobanana-output/ (read only — verify contents, do not create)</files>
  <what-built>Task 1 ran the nano-banana test generation command and produced NANO-BANANA-VERIFY.md. If the CLI was available and the API key was set, at least one PNG file should exist in ./nanobanana-output/.</what-built>
  <how-to-verify>
    1. Open .planning/phases/01-pre-production/NANO-BANANA-VERIFY.md and read the Status section
    2. If Status is PASS: Open the generated PNG file(s) in ./nanobanana-output/ and confirm:
       - The image looks like a dark architectural interior (not garbage, not a white square)
       - No text, logos, or watermarks are burned into the image
       - The aspect ratio is visually portrait/vertical (9:16 shape)
    3. If Status is FAIL or gemini was not in PATH: Read the Fallback Path section in the doc. Confirm the doc clearly states "Phase 2 will use Fitzrovia CDN photos exclusively."
    4. Either outcome is acceptable — the goal is knowing which path Phase 2 takes.
  </how-to-verify>
  <action>Human checkpoint — no automated action. Review outputs of Task 1 per the how-to-verify instructions above.</action>
  <verify>
    <automated>grep -q "Status" .planning/phases/01-pre-production/NANO-BANANA-VERIFY.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <resume-signal>Type "approved — CLI works" or "approved — fallback path confirmed" or describe any issue</resume-signal>
  <done>Human has read NANO-BANANA-VERIFY.md and confirmed either: CLI works and generated image is usable, or fallback path (CDN photos) is documented and Phase 2 executor will follow it.</done>
</task>

<task type="auto">
  <name>Task 3: Write META-HOUSING-BRIEF.md and FIGMA-TEMPLATE-SPEC.md</name>
  <files>
    .planning/phases/01-pre-production/META-HOUSING-BRIEF.md
    .planning/phases/01-pre-production/FIGMA-TEMPLATE-SPEC.md
  </files>
  <read_first>
    - .planning/phases/01-pre-production/01-RESEARCH.md (PRE-04: Meta Housing Special Ad Category section, Figma Safe-Zone Template Setup section)
    - .planning/research/PITFALLS.md (Pitfall 3: Instagram Story Safe Zone Violations, Pitfall 5: Meta Housing Ads Policy)
    - .planning/research/FEATURES.md (Table Stakes section)
  </read_first>
  <action>
**Create .planning/phases/01-pre-production/META-HOUSING-BRIEF.md:**

```
# Meta Housing Special Ad Category — Campaign Brief

**Status:** DOCUMENTED
**Date:** 2026-03-15
**Requirement ID:** PRE-04
**For:** Whoever sets up the Parker campaign in Meta Ads Manager

## CRITICAL: Do This First

When creating the Parker campaign in Meta Ads Manager, the VERY FIRST thing to
do is set the Special Ad Category. Before configuring any ad sets, audiences,
or creative:

1. Create a new Campaign
2. In the Campaign settings, find "Special Ad Categories"
3. Select: **Housing**
4. Only then proceed to configure ad sets and audiences

If you skip this step, Meta may reject ads after launch or force a campaign
restart that loses all your ad set configuration.

## What the Housing Category Restricts

These targeting options are LOCKED by Meta policy for all housing ads in Canada.
Do not attempt to use them — the fields will be greyed out in Ads Manager.

| Restriction | What It Means |
|-------------|--------------|
| Age targeting | Fixed at 18–65+ (cannot narrow to e.g. 25–45) |
| Gender targeting | Must include all genders |
| ZIP/postal code targeting | Not allowed |
| Geographic radius | Minimum ~25 km radius applies |
| Lookalike audiences | Standard Lookalike Audiences blocked |
| Detailed interest targeting | Most demographic and interest options removed |
| Audience exclusions | Exclusion targeting not allowed |

## What Still Works

These targeting approaches ARE available under the Housing category:

| Approach | Notes |
|----------|-------|
| Province / city targeting | Ontario, Toronto — still available |
| Custom audiences (first-party) | Website visitors via Meta Pixel — AVAILABLE |
| Retargeting: chat_opened event | Warm audience — people who opened the chatbot |
| Retargeting: chat_started event | Hot audience — people who sent a message |
| Suppression: lead_captured event | Exclude converted leads from ads |
| Special Ad Audiences | Meta's compliant alternative to Lookalike Audiences |

The Meta Pixel on parker.affordablecondos.ca fires these events:
- `ViewContent` — user opens chatbot
- `Lead` — user completes lead capture (equivalent to `lead_captured`)

Use these events to build retargeting and suppression audiences.

## Campaign Destination URL

All 5 ads link to: **https://parker.affordablecondos.ca**

Set this as the destination URL in each ad unit in Ads Manager.
The URL shown in the ad image is visual only — the actual click destination
is set here, in the ad configuration.

## Per-Ad CTA Mapping

| Ad | Image CTA Text | Destination |
|----|---------------|-------------|
| Ad 1 — Incentives | "Claim Your Free Month" | https://parker.affordablecondos.ca |
| Ad 2 — Lifestyle | "Book a Private Tour" | https://parker.affordablecondos.ca |
| Ad 3 — Location | "Explore Suites" | https://parker.affordablecondos.ca |
| Ad 4 — Urgency | "Move In Today" | https://parker.affordablecondos.ca |
| Ad 5 — Conversational | "Chat with Emma" | https://parker.affordablecondos.ca |

All five ads use the same destination URL. The CTA text in the image is visual
branding only — Ads Manager has its own CTA button field.

## File Naming (Phase 3 will confirm)

Expected final filenames for upload:
- parker-ad-01-incentives.png
- parker-ad-02-lifestyle.png
- parker-ad-03-location.png
- parker-ad-04-urgency.png
- parker-ad-05-conversational.png

## Source

Meta Special Ad Categories (Housing): https://www.facebook.com/business/help/298000447747885
Mandatory in Canada since December 2020.
```

**Create .planning/phases/01-pre-production/FIGMA-TEMPLATE-SPEC.md:**

```
# Figma Canvas Template Spec

**For:** Human to set up in Figma before Phase 2 compositing begins
**Date:** 2026-03-15
**Used by:** All 5 Phase 2 ad plans

## Canvas Setup

1. Create a new Figma file named: "Parker Story Ads — Phase 2"
2. Add a new Frame with:
   - **Width:** 1080px
   - **Height:** 1920px
   - **Name:** "Parker Story Ad — TEMPLATE"

## Guides to Add

Add these horizontal and vertical guides to the frame. In Figma: drag from
ruler, or use Edit → Guides → Add Guide with exact position.

| Guide | Direction | Position | Purpose |
|-------|-----------|----------|---------|
| Top safe line | Horizontal | y = 250px | Below Instagram profile bar / progress indicator |
| Bottom safe line | Horizontal | y = 1536px | Above Instagram swipe-up / link bar |
| Left margin | Vertical | x = 90px | Safe content left edge |
| Right margin | Vertical | x = 990px | Safe content right edge (1080 − 90) |

**Effective safe content area:** 900 × 1286px (x: 90–990, y: 250–1536)

## Unsafe Zone Overlay Layers (for QA — delete before export)

Add these rectangle layers inside the template frame, above the background
image layer but below any text layers:

**Top unsafe zone:**
- Rectangle: x=0, y=0, width=1080, height=250
- Fill: rgba(255, 0, 0, 0.3) (red semi-transparent)
- Name: "UNSAFE — DO NOT PLACE CONTENT"
- Lock this layer

**Bottom unsafe zone:**
- Rectangle: x=0, y=1536, width=1080, height=384
- Fill: rgba(255, 0, 0, 0.3) (red semi-transparent)
- Name: "UNSAFE — DO NOT PLACE CONTENT"
- Lock this layer

Delete both red layers before exporting any final PNG.

## Fonts to Load

Load these from Google Fonts in Figma before compositing:
- **Cormorant Garamond** — weights: 300 italic, 400, 400 italic, 600
- **Plus Jakarta Sans** — weights: 300, 400, 500, 600

To load in Figma: Text tool → Font picker → search by name → Figma loads
from Google Fonts automatically when connected to the internet.

## Layer Order (for each ad, bottom to top)

1. Background image (Fitzrovia CDN photo or nano-banana generated PNG)
2. Gradient overlay rectangle (see overlay recipes in FEATURES.md)
3. Unsafe zone QA layers (red rectangles — delete before export)
4. Parker logo (white SVG) — top-left of safe zone, approx y=270–340
5. Orange accent rule (40×2px, #E85B3A) — above eyebrow
6. Eyebrow text (Plus Jakarta Sans 500, 24–28px, all-caps, 0.25em tracking)
7. Headline (Cormorant Garamond 400, 96–140px, -0.01em tracking)
8. Subhead / supporting copy (Plus Jakarta Sans 300, 28–34px)
9. CTA button (rectangle #E85B3A, no border-radius) + button label (Jakarta 600, white, uppercase)
10. Attribution text (Jakarta 400, 18–20px, rgba(255,255,255,0.5), y=1480–1520px)

## Attribution Text Layer

Use this exact text in the attribution layer of every ad:

```
Garima Nagpal · Team Nagpal · Sales Representative · RE/MAX Hallmark Realty, Brokerage
```

Full spec: see .planning/phases/01-pre-production/RECO-ATTRIBUTION.md

## Export Settings (for each ad)

Format: PNG
Color profile: sRGB (confirm in Figma: File → Color Profile → sRGB)
Scale: 1× (1080×1920 at 1×)
Include background: YES

Verify after export:
```bash
sips -g pixelWidth -g pixelHeight parker-ad-01-incentives.png
# Expected: pixelWidth: 1080 / pixelHeight: 1920
```

## Checkpoint Before Phase 2

Before Phase 2 plans execute, confirm:
- [ ] Figma file created with TEMPLATE frame at 1080×1920
- [ ] Four guides added at y=250, y=1536, x=90, x=990
- [ ] Red unsafe zone rectangles are in place (can be toggled visible/hidden)
- [ ] Cormorant Garamond and Plus Jakarta Sans are loaded
- [ ] Color profile is sRGB
```
  </action>
  <verify>
    <automated>test -f .planning/phases/01-pre-production/META-HOUSING-BRIEF.md && test -f .planning/phases/01-pre-production/FIGMA-TEMPLATE-SPEC.md && grep -q "PRE-04" .planning/phases/01-pre-production/META-HOUSING-BRIEF.md && grep -q "Housing" .planning/phases/01-pre-production/META-HOUSING-BRIEF.md && grep -q "1080" .planning/phases/01-pre-production/FIGMA-TEMPLATE-SPEC.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - META-HOUSING-BRIEF.md exists and contains "PRE-04"
    - META-HOUSING-BRIEF.md has "Do This First" section with Housing category instructions as step 1
    - META-HOUSING-BRIEF.md has the targeting restrictions table
    - META-HOUSING-BRIEF.md has the per-ad CTA mapping table with all 5 ads
    - META-HOUSING-BRIEF.md has destination URL: https://parker.affordablecondos.ca
    - FIGMA-TEMPLATE-SPEC.md exists with 1080×1920 canvas spec
    - FIGMA-TEMPLATE-SPEC.md has all 4 guide positions (y=250, y=1536, x=90, x=990)
    - FIGMA-TEMPLATE-SPEC.md has layer order list
    - FIGMA-TEMPLATE-SPEC.md has the confirmed attribution string
    - FIGMA-TEMPLATE-SPEC.md has export settings (PNG, sRGB, 1×)
  </acceptance_criteria>
  <done>META-HOUSING-BRIEF.md and FIGMA-TEMPLATE-SPEC.md both exist with complete, actionable content. PRE-04 is cleared. Campaign manager has everything needed to avoid Housing category errors. Human has step-by-step Figma setup instructions for Phase 2.</done>
</task>

</tasks>

<verification>
All three output files exist:
```bash
ls -la .planning/phases/01-pre-production/NANO-BANANA-VERIFY.md .planning/phases/01-pre-production/META-HOUSING-BRIEF.md .planning/phases/01-pre-production/FIGMA-TEMPLATE-SPEC.md
```

Requirement IDs present:
```bash
grep -l "PRE-02\|PRE-04" .planning/phases/01-pre-production/*.md
```

Attribution string in Figma spec:
```bash
grep "RE/MAX Hallmark Realty, Brokerage" .planning/phases/01-pre-production/FIGMA-TEMPLATE-SPEC.md
```
</verification>

<success_criteria>
- NANO-BANANA-VERIFY.md exists with setup check results and either a PASS (CLI works + test image) or a FAIL with clear Fallback Path documented
- META-HOUSING-BRIEF.md exists with Housing category instructions, restriction table, CTA mapping, and destination URL
- FIGMA-TEMPLATE-SPEC.md exists with exact frame dimensions, guide positions, layer order, and export settings
- PRE-02 and PRE-04 requirement IDs are present in their respective docs
- Phase 2 executor knows exactly which background source to use (CLI or CDN photos) based on NANO-BANANA-VERIFY.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-pre-production/01-02-SUMMARY.md`
</output>
